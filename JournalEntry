{
  "name": "JournalEntry",
  "type": "object",
  "properties": {
    "plant_id": {
      "type": "string",
      "description": "Associated plant ID"
    },
    "title": {
      "type": "string"
    },
    "content": {
      "type": "string",
      "description": "Journal entry content"
    },
    "photo_url": {
      "type": "string"
    },
    "entry_date": {
      "type": "string",
      "format": "date"
    }
  },
  "required": [
    "title",
    "entry_date"
  ],
  "rls": {
    "create": {
      "created_by": "{{user.email}}"
    },
    "read": {
      "created_by": "{{user.email}}"
    },
    "update": {
      "created_by": "{{user.email}}"
    },
    "delete": {
      "created_by": "{{user.email}}"
    },
    "write": {
      "created_by": "{{user.email}}"
    }
  }
}
